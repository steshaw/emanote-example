# For documentation and available settings, see
# https://github.com/srid/emanote/blob/master/default/index.yaml

page:
  siteTitle: Steven Shaw
  siteName: emanote-example

template:
  theme: blue
  sidebar:
    collapsed: false
  urlStrategy: pretty
